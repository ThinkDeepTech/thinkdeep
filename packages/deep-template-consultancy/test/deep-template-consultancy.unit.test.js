const chai = require('chai'),
  expect = chai.expect;

describe('deep-template-consultancy', () => {
  // TODO: Pull old attach lib for testing into this project.
  // beforeEach(() => {
  //     attach(element);
  // });

  it('should correctly find this test', () => {
    expect(true).to.equal(true);
  });
});
