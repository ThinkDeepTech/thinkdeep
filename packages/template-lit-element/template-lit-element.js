import { css, html, LitElement, customElement } from 'lit-element';

/* eslint-disable no-unused-vars */
@customElement('package-name')
class PackageName extends LitElement {
  static get styles() {
    return css`
      // TODO: Style definitions
    `;
  }

  render() {
    return html`
      ${this.styles}

      <!-- TODO: Markup -->
    `;
  }
}
