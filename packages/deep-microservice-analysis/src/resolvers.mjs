
const resolvers = {
    // sentiments: async (_, {economicEntityName, economicEntityType}, { dataSources, permissions}) => await dataSources.analysisService.computeSentiment(economicEntityName, economicEntityType, permissions)
};

export { resolvers };