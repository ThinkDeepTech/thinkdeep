apiVersion: "v1"
kind: "Secret"
metadata:
    name: {{ printf "%s" ( required "A kafka secret name is required." .Values.global.kafka.secretName) | quote }}
    namespace: {{ .Values.global.namespace | quote }}
data:
    PREDECOS_KAFKA_HOST: {{ printf "kafka-latest.%s.svc.cluster.local" .Values.global.namespace | b64enc | quote}}
    PREDECOS_KAFKA_PORT: {{ printf "%d" (required "A valid kafka port is required if a secret name is not specified." .Values.global.kafka.port) | b64enc }}